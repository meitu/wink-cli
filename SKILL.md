---
name: wink-cli
description: 通过本机 Wink CLI 动态发现并执行当前客户端支持视频图片处理能力。用户提到 Wink CLI、wink --help、wink cli、项目 CLI、云工具箱 CLI，或要求用命令行处理图片/视频时使用。
---

# Wink CLI

使用本机安装的 `wink` 命令执行Wink视频图片处理能力。skill 本身不固定任何功能、档位或参数；实际命令面必须以当前机器 `wink --help` 和子命令帮助输出为准。

## 安装依赖

如果运行`wink`命令提示**找不到命令**，可能是未正确安装相关依赖，可以先尝试执行`npx wink-cli install`

npx安装完后运行`wink`命令依旧**找不到命令**，则尝试引导用户安装wink软件，根据用户当前操作系统语言分别引导
- 对中文用户，引导用户打开官网 `https://wink.cn/` ，提示用户先下载并安装wink软件
- 对非中文用户，引导用户打开官网 `https://wink.ai/` ，提示用户先下载并安装wink软件


## 必做流程

1. 先运行 `wink --help`，确认 CLI 可用，并从输出中读取当前版本支持的功能命令、全局选项、共享工具选项、输出路径规则和退出码。
2. 根据用户意图在帮助输出的功能命令中选择最匹配的子命令；如果没有匹配命令，向用户说明当前客户端不支持该能力。
3. 执行前必须运行 `wink <command> --help`，从子命令帮助中读取用法、必填参数、可选参数、档位/模式等取值范围和示例。
4. 只使用帮助中明确存在的参数生成命令。不要把旧版本、文档或记忆中的参数写进命令。
5. 生成命令时优先使用 `wink <command> ...`。如果 `wink --help` 不可用但已定位到 `Wink.exe`，则使用 `Wink.exe --cli <command> ...`。
6. 路径一律使用绝对路径并加双引号。不要猜测输入文件；缺少必填参数或取值无法从用户意图确定时，先向用户确认。

## 动态决策

解析 `wink --help` 时关注这些信息：

- 功能命令列表：用于判断当前客户端支持哪些能力。
- 全局选项：例如 JSON 输出、超时、token、配置文件、结果文件等，以实际输出为准。
- 共享工具选项：例如输入、输出、档位、异步、任务 ID、进度文件等，以实际输出为准。
- 退出码：失败时按当前版本的退出码说明解释。
- 输出路径规则：向用户汇报结果路径时以当前版本说明和命令返回为准。

选择命令时不要硬编码映射表。用用户意图中的关键词、帮助里的命令名和命令说明进行匹配，例如“画质修复”应匹配帮助中最接近的画质修复命令；匹配后再读取该命令自己的 `--help`。

## 命令生成

读取 `wink <command> --help` 后，按下面顺序生成命令：

1. 从子命令帮助的“用法”中确定命令骨架。
2. 从帮助中找出必填参数，并用用户提供的信息填入。
3. 从帮助中找出可选参数，只在用户需求明确、或对长任务明显有帮助时使用。
4. 如果帮助列出了档位、模式或枚举取值，只能选择其中存在的取值；用户没有指定时使用帮助中标注的默认值，或不传该参数。
5. 如果用户要求后台处理，只有在帮助中存在异步参数时才添加异步参数；同时按帮助中的任务查询方式保留 `task_id`、进度文件或结果文件。
6. 如果用户要求指定输出位置，只有在帮助中存在对应输出参数时才传入；最终仍以命令返回的结果路径为准。

命令模板只表达结构，不代表固定参数：

```bash
wink <command> <required-options-from-help> <optional-options-from-help>
```

示例结构：

```bash
wink <matched_command> --input "D:/absolute/input.ext"
```

如果子命令帮助显示支持异步：

```bash
wink <matched_command> <options-from-help> <async-option-from-help> <task-id-option-from-help> "wink_cli_<short_id>"
```

## 任务管理

如果用户要查询、取消或清理任务，也先从 `wink --help` 中查找当前版本是否提供对应任务管理命令，再运行对应 `wink <task-command> --help` 生成命令。不要假定命令名固定。

## 错误处理

- 退出码含义以本次 `wink --help` 输出为准。
- 参数无效时，重新查看 `wink --help` 和子命令帮助，修正参数。
- 未登录、权益不足、云端失败、输入无效、超时、取消、内部错误等场景，优先使用命令返回 JSON 和帮助中的退出码说明解释。
- 保留 stdout/stderr、`task_id`、进度文件、结果文件等当前版本返回的排查信息。
- 如果同步任务超时，且帮助中存在异步能力，建议改用异步后再查询任务状态。

## 输出给用户

执行完成后简洁说明：

- 使用的命令和关键参数。
- 任务是否成功。
- 输出文件路径或任务 ID。
- 如果失败，给出退出码、错误字段和下一步处理建议。
